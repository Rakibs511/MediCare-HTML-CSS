import { createSlice } from "@reduxjs/toolkit"

const initialState={
    phoneNumber:"",
    password:""
}

export const signUpSlice = createSlice({
    name:'signUp',
    initialState,
    reducers:{
        
    }
})